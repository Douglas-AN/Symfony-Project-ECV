{% extends 'base.html.twig' %}

{% block title %}Sport index
{% endblock %}

{% block body %}
	<style></style>
	<h1>Liste des sports</h1>

	{# <table class="table">
			<thead>
				<tr>
					<th>Name</th>
					<th>actions</th>
				</tr>
			</thead>
			<tbody>
				{% for sport in sports %}
					<tr>
						<td>{{ sport.name }}</td>
						<td>
							<a class="btn btn-dark" href="{{ path('app_sport_show', {'id': sport.id}) }}">show</a>
							<a class="btn btn-primary" href="{{ path('app_sport_edit', {'id': sport.id}) }}">edit</a>
						</td>
					</tr>
				{% else %}
					<tr>
						<td colspan="3">no records found</td>
					</tr>
				{% endfor %}
			</tbody>
		</table> #}
	<a class="btn btn-success" href="{{ path('app_sport_new') }}">Create new</a>

	<div class="container-sport">
		{% for sport in sports %}
        <div class="card">
			<figure class="card__thumb">
				<img src="{{ sport.url }}" alt="Picture by Kyle Cottrell" class="card__image">
				<figcaption class="card__caption">
					<h2 class="card__title">{{ sport.name }}</h2>
					<p class="card__snippet">{{ sport.description }}</p>
					<a href="{{ path('app_sport_show', {'id': sport.id}) }}" class="card__button">Voir la liste des joueurs</a>
				</figcaption>
			</figure>
		</div>
			
		{% else %}
			<tr>
				<td colspan="3">no records found</td>
			</tr>
		{% endfor %}
	</div>

{% endblock %}
